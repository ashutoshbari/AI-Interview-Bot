{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "env": {
    "NEXT_PUBLIC_API_URL": {
      "description": "URL of your deployed FastAPI backend on Render",
      "required": true
    }
  }
}
